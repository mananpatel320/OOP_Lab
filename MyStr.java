class MyStr{
	public static void main(String[] args){
		stringReverse(args[0],0);
	}
	static void stringReverse(String s, int f){
		if(f == (s.length())/2){
			System.out.println(s);
		}
		else{
			//char temp;
			//temp = s.charAt(f);
			//s.charAt(f) = s.charAt(s.length()-1-f);
			//s.charAt(s.length()-1-f) = temp;
			//stringReverse(s,f+1);
			System.out.println(			
		}
	}
}
